#!/bin/bash

if [ `whoami` != root ]; then
    echo Please run this script as root or using sudo
    exit
fi

silversearcher-ag expect git gdisk htop ssh vsftpd samba vim filezilla nautilus-dropbox bind9 jump supervisor vim vnc4server vncpasswd nginx

### Boinc ###
# seti/boinc cliet
# apt-get install python-software-properties
# add-apt-repository ppa:costamagnagianfranco/boinc
# apt-get update
# apt-get install boinc-client
# /etc/default/boinc-client
# boinccmd

