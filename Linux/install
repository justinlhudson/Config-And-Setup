#!/bin/bash

if [ `whoami` != root ]; then
    echo Please run this script as root or using sudo
    exit
fi

# apt-get Ubuntu distro version
silversearcher-ag expect git gparted gdisk htop ssh vsftpd samba vim filezilla nautilus-dropbox bind9 jump supervisor vim vnc4server vncpasswd nodejs npm nginx
redis-server

# latest per website instructions
mongodb
docker (docker-engine)
mono (mono-devel, mono-complete)
oh-my-zsh

#node verse nodjs
sudo ln -s /usr/bin/nodejs /usr/bin/node

sudo npm install bower -g
sudo npm install gulp -g

### Boinc ###
# seti/boinc cliet
# apt-get install python-software-properties
# add-apt-repository ppa:costamagnagianfranco/boinc
# apt-get update
# apt-get install boinc-client
# /etc/default/boinc-client
# boinccmd

