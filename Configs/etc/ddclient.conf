# Configuration file for ddclient generated by debconf
# Check:  sudo ddclient -query
# /etc/ddclient.conf

# matches dyndns TTL
daemon=600
ssl=yes
protocol=dyndns2
use=web, web=checkip.dyndns.com, web-skip='IP Address'
server=members.dyndns.org
login=<username>
password=<password>
<urls ...>
