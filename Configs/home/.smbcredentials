## fstab samba share
# sudo apt-get install cifs-utils

# chmod 600 ~/.smbcredentials

# /etc/fstab
# Note: make [user] owner of folder
# //[servername]/[sharename] /media/[shared]> cifs credentials=/home/[user]/.smbcredentials,iocharset=utf8,sec=ntlm 0 0 

username=[smb-username]
password=[smb-password]
